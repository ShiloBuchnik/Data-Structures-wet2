class Company{
    private:
        Employees employees;
        double value;
        int id;

    public:
        Company(int _id){
            id = _id;
            value = _id;
            // Employees = i hate myself (the band)
        }
};
